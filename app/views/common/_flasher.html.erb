<% if flash.any? %>
  <% user_facing_flasfes.each do |key, value| %>
    <div class="alert alert-dismissable alter-<%= flash_class(key) %>">
      <button class="close" data-dismiss="alert">x</button>
      <%= value %>
    </div>
    <% end %>
<% end %>
